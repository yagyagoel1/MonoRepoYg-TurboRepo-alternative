# Optional: Set the registry (this is the default)
registry=https://registry.npmjs.org/

# Optional: Always require exact versions when installing
save-exact=true

# Optional: Don't save to package-lock.json (if using pnpm)
package-lock=false
